/**
 * Реэкспорт storage-функций из shared/blueprint для обратной совместимости.
 */
export {
  deleteBlueprint,
  listBlueprints,
  loadBlueprint,
  saveBlueprint,
} from '@/shared/blueprint/blueprint-storage';
